<Window x:Class="EveIndustryPlanner.App.ProductionAssetsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:EveIndustryPlanner.App"
        Title="Production Ledger Assets"
        Width="1120"
        Height="720"
        MinWidth="900"
        MinHeight="560"
        Background="#f3f4f6"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI" />
            <Setter Property="Foreground" Value="#172033" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="32" />
            <Setter Property="Padding" Value="12,4" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="MinHeight" Value="30" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Padding" Value="6,2" />
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Margin="0,0,0,10">
            <TextBlock Text="Production Ledger Assets"
                       FontSize="20"
                       FontWeight="SemiBold"
                       DockPanel.Dock="Left" />
            <TextBlock Text="{Binding SummaryText}"
                       Foreground="#526176"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Center" />
        </DockPanel>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="220" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Search" FontSize="12" Foreground="#526176" />
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <StackPanel Grid.Column="2">
                <TextBlock Text="Location" FontSize="12" Foreground="#526176" />
                <TextBox Text="{Binding LocationFilterText, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <CheckBox Grid.Column="4"
                      Content="Needed only"
                      IsChecked="{Binding NeededOnly}"
                      VerticalAlignment="Bottom"
                      Margin="0,0,12,6" />

            <Button Grid.Column="6"
                    Content="Refresh Assets"
                    Command="{Binding RefreshCommand}"
                    Margin="0,0,8,0"
                    VerticalAlignment="Bottom" />
                <Button Grid.Column="7"
                    Content="Select Matching"
                    Command="{Binding SelectNeededCommand}"
                    VerticalAlignment="Bottom" />
        </Grid>

        <Border Grid.Row="2"
                Background="White"
                BorderBrush="#8aa5d1"
                BorderThickness="1">
            <TreeView ItemsSource="{Binding Systems}"
                      BorderThickness="0">
                <TreeView.Resources>
                    <HierarchicalDataTemplate DataType="{x:Type local:ProductionAssetTreeNode}"
                                              ItemsSource="{Binding Children}">
                        <Grid Margin="0,1">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30" />
                                <ColumnDefinition Width="240" />
                                <ColumnDefinition Width="110" />
                                <ColumnDefinition Width="95" />
                                <ColumnDefinition Width="95" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      Visibility="{Binding SelectionVisibility}"
                                      VerticalAlignment="Center" />
                            <TextBlock Grid.Column="1"
                                       Text="{Binding Name}"
                                       FontWeight="{Binding HeaderWeight}"
                                       TextTrimming="CharacterEllipsis" />
                            <TextBlock Grid.Column="2"
                                       Text="{Binding NodeType}"
                                       Foreground="#526176" />
                            <TextBlock Grid.Column="3"
                                       Text="{Binding NeededTypeCount, StringFormat=N0}" />
                            <TextBlock Grid.Column="4"
                                       Text="{Binding AppliedQuantity, StringFormat=N0}" />
                            <TextBlock Grid.Column="5"
                                       Text="{Binding MatchingItemsPreview}"
                                       Foreground="#526176"
                                       TextTrimming="CharacterEllipsis" />
                        </Grid>
                    </HierarchicalDataTemplate>
                </TreeView.Resources>
            </TreeView>
        </Border>

        <DockPanel Grid.Row="3" Margin="0,12,0,0">
            <TextBlock Text="{Binding AppliedSummaryText}"
                       Foreground="#526176"
                       VerticalAlignment="Center"
                       DockPanel.Dock="Left" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Cancel"
                        Width="100"
                        IsCancel="True"
                        Margin="0,0,8,0" />
                <Button Content="Apply Selected"
                        Width="130"
                        Command="{Binding ApplyCommand}" />
            </StackPanel>
        </DockPanel>
    </Grid>
</Window>
