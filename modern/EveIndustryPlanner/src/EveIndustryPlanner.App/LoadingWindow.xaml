<Window x:Class="EveIndustryPlanner.App.LoadingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Eve Industry Planner"
        Width="460"
        Height="190"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="#f7f9fc"
        ShowInTaskbar="False"
        Topmost="True">
    <Border BorderBrush="#cbd5e1"
            BorderThickness="1"
            Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="Eve Industry Planner"
                       FontSize="22"
                       FontWeight="SemiBold"
                       Foreground="#0f1f3a" />
            <TextBlock Grid.Row="1"
                       x:Name="StatusTextBlock"
                       Text="Starting application"
                       Margin="0,10,0,0"
                       Foreground="#314363"
                       FontSize="13" />
            <ProgressBar Grid.Row="2"
                         Margin="0,22,0,0"
                         Height="8"
                         IsIndeterminate="True" />
            <TextBlock Grid.Row="3"
                       Text="Preparing local data and server state."
                       Margin="0,16,0,0"
                       Foreground="#64748b"
                       FontSize="12" />
        </Grid>
    </Border>
</Window>
